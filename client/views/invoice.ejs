<% include ./partials/header %>
<div class="container text-center">
    <div class="row">
        <div class="invoices">
            <h1>Facturas Descargadas</h1>
            <!--
            <form action="/logs" method="post">
                <div class="form-group">
                    Message: <input type="text" class="form-control" name="log"><br>
                </div>
                <div class="form-group">
                    <input type="submit" class="form-control" value="Mandalo alv">
                </div>
            </form>-->
        </div>
        <div>
            <% xmlArray.forEach(function(invoice, i) { %>
            <div class="jumbotron xml">
                <h2>Factura
                    <%=i+1%>
                </h2>
                <%=invoice %>
            </div>
            <% }) %>
        </div>
    </div>
</div>
</body>

</html>